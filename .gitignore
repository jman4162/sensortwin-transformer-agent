# Private planning / spec / reference docs (not for public remote)
BACKGROUND-SPEC-GITIGNORE.md
AI_WRITING_SLOP_Guide.md

# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
env/
.python-version

# Tooling caches
.pytest_cache/
.ruff_cache/
.mypy_cache/
.ipynb_checkpoints/

# Generated data, checkpoints, run artifacts
# NOTE: anchored to repo root with a leading slash so it does NOT match the
# sensortwin/data/ source package.
/data/
*.npz
*.parquet
*.pt
*.ckpt
runs/
wandb/
reports/figures/*
!reports/figures/.gitkeep
# Regenerable per-run artifacts (the human-readable *_results.md reports are kept).
reports/experiment_summaries/figures/
reports/experiment_summaries/metrics_*.json
reports/experiment_summaries/metrics_*.csv
# Ablation table needs colab_standard + seed averaging to be meaningful; generate with `make ablate`.
reports/experiment_summaries/transformer_ablations.md

# OS / editor
.DS_Store
.idea/
.vscode/
